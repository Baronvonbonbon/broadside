{
  "name": "@broadside/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Broadside contracts, compiled to PolkaVM via resolc.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "node scripts/deploy.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "ethers": "^6.17.0"
  }
}
